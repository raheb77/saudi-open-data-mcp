services:
  app:
    build:
      context: .
    # Intentionally uses the source-tree CLI for startup validation only.
    # This compose file is not the serving deployment path yet.
    command: ["python", "src/saudi_open_data_mcp/cli.py", "check-startup"]
