services:
  app:
    build:
      context: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - saudi-open-data-mcp-data:/var/lib/saudi-open-data-mcp
    restart: unless-stopped

volumes:
  saudi-open-data-mcp-data:
